# 麻将 AI 项目 —— 现状诊断与修正方案

> 约束：**不改动当前神经网络架构**（256×4×9 输入、18 层 FiLM-ResBlock、4 层 Transformer 序列编码器、Cross-Attention 融合、54 维 policy 头 + value + 3 个 aux 头，全部冻结）。
> 本方案只动 **数据 / 标签 / 特征编码 / 动作空间 / 环境 / 奖励 / 评估 / 工程** 这条基础链。
>
> 生成日期：2026-08-18　　基线代码：`1ac660a`

---

## 0. 结论摘要（先读这一节）

两份参考书都指出"训练大概率出不了结果"，方向正确，但都**没有指出真正的致命闭环**。实测（见 §1）证明：

> **当前模型是一个"从不和牌、逢鸣必鸣"的退化策略。它不是训得慢，是原理上不可能赢。**

死亡闭环（三个 bug 串成一条链）：

```
data_builder 从不产出 reach / hora / pass 样本
        ↓
SL 的 47 维 policy 头里 41(Riichi) 42(Ron) 43(Tsumo) 44(九种) 45(Pass) 46(不立直)
从未收到过任何正样本 → 权重死亡（‖w‖≈0.308, bias≈-2.03，与初始化无异）
        ↓
adapt_policy_state_dict 把这些死权重原样搬进 54 维空间的 48/49/50/51/52/53
        ↓
① 能荣和时 RON(49) logit = -1.23 < PASS(53) logit = -0.61 → 放弃和牌
② 鸣牌类 43/44(Pon) bias=+0.01、37~42(Chi) bias≈-0.03，而 PASS(53) bias=-1.96
   → 逢鸣必鸣（实测鸣牌机会命中率 79%，杠 100%）
        ↓
门前率≈0 → 永不听牌 → 立直机会 0 次 / 1485 步 → 自摸机会 0 次
        ↓
每局必然流局。和了率 = 0.00%（eval log 8 次评估全部 ≤0.1%）
        ↓
PPO 拿不到任何"和牌"的奖励样本 → value 头学不到和牌存在
KL 惩罚又把策略死死锚在这个死先验上 → 永远出不来
        ↓
avg_rank 1.936 的"好成绩"来自流局听牌罚符和对手同样不会和牌，
Phase 2→3 的 win_r >= 0.05 门槛在此结构下**永远无法满足**
```

**因此：现在这轮 PPO（已跑到 iter 4189）应当立即停止，任何超参调整都是浪费。**

修复顺序必须是：
**① 打通"会和牌 / 会拒绝鸣牌"通路 → ② 重建数据与标签 → ③ 特征双管线一致性 → ④ 重训 SL → ⑤ 重做 RL 环境与评估**。

---

## 1. 实测证据（本次新增，两份参考书均未涵盖）

### 1.1 checkpoint 权重体检

`smart_mahjong_base_policy_v2.pth` 实际是 **47 维**头（不是 54），逐 index 的 `bias / ‖w‖`：

| index（pymahjong v1.0 语义） | bias | ‖w‖ | 判定 |
|---|---|---|---|
| 0–33 打牌 | -0.42(均) | 0.887 | 已训练 |
| 34 Chi(小) | -0.02 | 0.895 | 已训练 |
| 37 Pon | +0.01 | 1.023 | 已训练 |
| 39 Kan | -0.12 | 0.666 | 已训练 |
| 35,36,38,40 | -2.00 | **0.308** | **死** |
| **41 Riichi / 42 Ron / 43 Tsumo / 44 九种 / 45 Pass / 46 不立直** | **-2.03** | **0.308** | **全死** |

跑完 4189 轮 PPO 之后（`smart_mahjong_ppo_latest_phase2_iter4180.pth`），48/49/50 仍是 `bias≈-2.00, ‖w‖≈0.34~0.42` —— **4000 多轮强化学习完全没能把和牌动作救回来**。

### 1.2 数据集体检（`data/train_dataset.h5`, 2 376 956 样本）

| 检查项 | 实测 |
|---|---|
| `target_score` 非零比例 | **0 / 200000（全零）** |
| `target_actions` 直方图 | PASS 192371（且被 m_act=0 屏蔽）、CHI 3168、PON 4446、KAN **15**、RIICHI **0**、HORA **0** |
| `target_waits` 正例率 | 0.75% |
| `target_tenpai` / `target_danger` | 全零占位 |
| 使用的牌谱文件数 | **5000 / 184425（仅 2.7%）** |
| 磁盘占用 | train 91 GB + val 10 GB（`state_2d` float32，99.99% 的值是 0/1） |

### 1.3 在线行为探针（phase2 TRUE_BEST，1485 个 p0 决策步）

| 动作 | 出现机会 | 实际选择 | 命中率 |
|---|---|---|---|
| Pon (43/44) | 60 | 54 | **90%** |
| Chi (37–42) | 103 | 56 | 54% |
| Kan (45/46/47) | 11 | 11 | **100%** |
| **Ron (49)** | 1 | **0** | **0%**（RON logit -1.229 vs PASS -0.611） |
| **Tsumo (50)** | **0** | 0 | 从未听牌自摸 |
| **Riichi (48)** | **0** | 0 | 门前率≈0，1485 步内一次立直机会都没有 |

同一探针跑**纯 SL 基线** `smart_mahjong_base_policy_v2.pth`（1006 步）：

| 动作 | 出现机会 | 实际选择 | 命中率 |
|---|---|---|---|
| Chi / Pon / Kan 合计 | 164 | 99 | **60.4%** |
| **Tsumo (50)** | 1 | **0** | **0%**（TSUMO logit -1.781 vs 选中的打牌 -1.779 —— 两个死权重的掷硬币） |
| Riichi (48) | 0 | 0 | — |

两点结论：
1. **问题的源头在 SL，不在 PPO。** SL 基线本身就不会和牌、鸣牌率已达 60%。
2. **PPO 把它训得更糟。** 鸣牌命中率 60.4% → 76.7%，因为"鸣牌 → 向听数下降 → shanten/ukeire shaping 奖励"这条捷径被 RL 充分利用，而"和牌"这条真正的路径被死权重堵死。**当前的 shaping 奖励在这个残缺动作空间下是有害的。**

复现命令：

```bash
.venv/Scripts/python.exe probe_policy_behavior.py <checkpoint.pth> 6000
```

---

## 2. 对两份参考书的校正

### 2.1 成立、且必须做的（两份共识）

- `target_score` / `target_tenpai` / `target_danger` 全零占位 —— **确认**（§1.2）。
- `decode_obs_93_to_256` 的 216–219 用**绝对座次**（`rl_ppo_trainer.py:493`），SL 侧用相对座次（`feature_extractor.py` 的 `216 + rel_seat`）—— **确认**。
- 213/214/215/220（本场/供托/残り牌/オーラス）RL 侧恒 0 —— **确认**。
- `action_id == 46` 被当成立直（`rl_ppo_trainer.py:555`）—— **确认是 bug**。查 `pymahjong/env_pymahjong.py:58`：`ANKAN=45, MINKAN=46, KAKAN=47, RIICHI=48`。**现在这行是在给"明杠"发立直奖励**。
- 采样端 `astype(np.int8)`（`rl_ppo_trainer.py:813`）—— **确认，且危害被低估**。pymahjong 的 obs_93 本身全是 0/1，decode 之后**唯一的非二值通道就是 211/212（风）和 216–219（点差）**，int8 截断把它们全部抹成 0，而训练端用的是原始 float32 → 重要性采样比从第 0 个 epoch 起就是错的。
- Rollout 推理时模型处于 `train()` 模式（`rl_ppo_trainer.py:1040` 之后再无 `.eval()`）—— **确认**。18 个 `Dropout2d(p=0.30)` 全程生效 + BatchNorm 用 micro-batch 统计量。
- GAE 末端自举 `next_val = values[-1]`（`rl_ppo_trainer.py:1009`）—— **确认**。
- 牌谱解析缺 `ankan` / `kakan`（`data_builder.py:234` 只处理 chi/pon/daiminkan）—— **确认**，且实测数据里 kakan 确实存在。`meld_types` 从未 append → `feature_extractor.py` 里 `m_type` 一律 fallback 成 `"pon"`，chi/ankan/minkan 三组通道在 SL 数据里恒空。
- 无 PASS 负样本 —— **确认，且这是致命闭环的一半**（§1.3 实测鸣牌命中率 79%）。
- 三套互不兼容的 seq_hist 编码 —— **确认**，见 §3.2-F。
- 数据增强的 `perm` 应为 `perm⁻¹`（`supervised_trainer.py:178`）、增强不同步 `t_waits`、`flip` 破坏宝牌指示牌关系 —— **确认**。
- 平台期判据 `cv_val = std/(|mean|+1e-8)`（`rl_ppo_trainer.py:1431`）在 avg_reward 绕 0 震荡时不可靠 —— **确认**（实测 avg_reward ≈ 0.011，分母极小）。
- HDF5 存储方案（float32 + `chunks=(128,256,4,9)` 无压缩）是最大工程瓶颈 —— **确认**（91 GB / 5000 文件；要用满 184425 个文件将需要约 3.3 TB）。
- WinRate / DealInRate 的口径（分母 = 总局数）没问题 —— **同意 ChatGPT 文档的撤回**。

### 2.2 参考书中**不成立 / 需要修正**的判断

| 出处 | 原判断 | 实际情况 |
|---|---|---|
| Claude 文档 P0-2 | `adapt_policy_state_dict` 因 `w.shape[0]==54` 提前 return，转换逻辑永不执行，SL 权重被"张冠李戴" | **不成立（对当前 ckpt）**。磁盘上的 `smart_mahjong_base_policy_v2.pth` 是 **47 维**，转换正常执行；而 `act_map=[45,34,37,39,41,42]` 恰好**完全吻合 pymahjong v1.0 的 47 动作定义**（见 `observation_action_explanation.pdf` Table 5）。当年的语义映射是对的。**但这是一颗定时炸弹**：`supervised_trainer.py:123` 现在已经是 `Linear(1024,54)` 而 `act_map` 还停在 47 空间 —— **只要你现在重训一次 SL，Claude 文档描述的错位就会 100% 变成现实**（CHI→34=赤5m 打出、PON→37=chi 左、RIICHI→41=chi 中赤…）。修 §3.1 时必须一起处理。 |
| Claude 文档 P0-6 | `_reset_hand_internal` 直接 `env.reset()` → 无亲家轮转，p0 永远是亲家，avg_rank 2.40 有系统性偏差 | **前提错误**。`pymahjong.MahjongEnv.reset()` 默认 `oya = self.game_count % 4`，而 `game_count` 每次 reset 自增 → **亲家是正常轮转的**。真正成立的部分是：**点数不继承（每局都回到 25000）、本场/供托不继承、`game_wind` 恒为 east（不存在南场与オーラス）**。结论方向对，理由要改。 |
| Claude 文档 P0-2 附注 | "`step()` 注释 46=立直 与 `adapt` 注释 48=立直 至少有一个是错的" | **已定案**：48 是对的（`env_pymahjong.py:58 RIICHI=48`），`rl_ppo_trainer.py:555` 的 46 是错的。 |
| ChatGPT 文档 P0-06 / P2-Action Space | "action space 本身并不是简单的 54 类分类，需要建立正式 schema" | **成立且优先级要提高**。补充：真源不是自己定义，而是 `pymahjong.MahjongEnv` 的类常量（`CHILEFT=37 … PASS_RESPONSE=53`）；`actions.py` 应当直接 import 并断言，而不是再抄一遍。 |
| ChatGPT 文档 P1-23 | "非法动作在训练阶段理论上永不发生" | **成立**，但注意现在的 `step()` 遇到非法动作会 `return ..., True, ...` 直接**结束半荘**，会把一整条轨迹污染成假 done。应改为 `assert`。 |

### 2.3 两份参考书**都遗漏**的问题（本次新发现）

| # | 位置 | 问题 |
|---|---|---|
| N-1 | 全局 | **死动作闭环**（§0/§1）。这是唯一能解释"和了率 0.00%"的原因，也是所有其它问题的放大器。两份文档都没有看训练日志与权重。 |
| N-2 | `rl_ppo_trainer.py:437-460` | **カベ（壁/无筋）通道是坏的**。`vis = obs[0]+obs[6]+obs[12]+…+obs[70]` 把一堆"≥1"的**布尔指示位**相加当成"可见枚数"，然后判 `vis==4`。obs_93 全是 0/1，这个和最大只有 10 且与真实枚数无关 → RL 侧 `st_base+2` 通道基本恒为 0/乱码，而 SL 侧 `feature_extractor.py` 用真实 `visible_counts` 算得是对的。**又一处双管线语义不一致，且是纯 bug。** |
| N-3 | `data_builder.py:203-212` | `cut_type` 取的是**当前这一手**的手切/摸切标志，却被写进 seq_hist 里**全部 72 个历史 token**。整条历史序列的"手切/摸切"位是错的。 |
| N-4 | `feature_extractor.py:345-348` vs `rl_ppo_trainer.py:648` | seq_hist 的截断方向相反：SL 取 `global_discards` 的**前 72 个**（最老），RL 取 `action_history[-72:]`（**最新 72 个**）。中盘以后两条管线看到的是完全不同的历史。 |
| N-5 | `rl_ppo_trainer.py:420` | 副露通道：RL 把 obs 的枚数平面直接填进 `st_base+0..3`，而 SL 那 4 个通道语义是**"チー"**。→ **RL 侧模型把所有碰和杠都看成吃**。 |
| N-6 | `rl_ppo_trainer.py:433` | `st_base+9..12` SL 语义是"立直后的舍牌"，RL 填的是"所有舍牌 − 立直宣言牌"。 |
| N-7 | `rl_ppo_trainer.py:638` vs `feature_extractor.py` 的 `cond_vec[12..15]` | 本场/供托/残り牌/オーラス 在 RL 侧恒 0，SL 侧有值。 |
| N-8 | `data_builder.py:317` | `max_files=5000` 只用了 **2.7%** 的牌谱。手上有 184425 个文件。 |
| N-9 | `data_builder.py` 全局 | MJAI 日志里 `start_kyoku` 自带 `scores` / `oya` / `kyoku` / `honba` / `kyotaku`，`hora`/`ryukyoku` 自带 **`deltas`** —— **修 `target_score` 所需的一切数据本来就在文件里**，只是没读。同理 `self_wind`（=`(seat-oya)%4`）、`players[].score`、`is_all_last`、`tiles_left`（数 tsumo 事件）全都可以零成本填上。 |
| N-10 | `rl_ppo_trainer.py:1040` + 主循环 | `update_from_buffer` 里的 `self.model.train()` 之后再没有 `.eval()`。（评估 worker 内部单独 `.eval()` 了，所以评估本身没问题，但采样端全程带 dropout。） |
| N-11 | `rl_ppo_trainer.py:530-536` | 非法动作时 `return ..., done=True`，会把该 worker 的整条轨迹以假 done 截断，且给 p0 记 -1 奖励，掩盖 mask bug。 |

---

## 3. 修正方案

按依赖顺序分 6 个阶段。**阶段 0/1 做完就能立刻看到和了率从 0% 跳起来**，这是最高性价比的一段。

---

### 阶段 0 —— 止血（4 处改动，约 1 小时，立即执行）

先停掉当前训练进程。

| # | 文件:行 | 改法 |
|---|---|---|
| S0-1 | `rl_ppo_trainer.py:1352` 附近（中央推理块） | 推理前 `trainer.model.eval()`，`update_from_buffer` 结束后再切回。或在 `update_from_buffer` 末尾加 `self.model.eval()`。**dropout 0.30 + BN micro-batch 统计让 old_log_prob 完全是噪声。** |
| S0-2 | `rl_ppo_trainer.py:813-816` | 删掉 `.astype(np.int8)`。要省带宽就用 `float16`（211/212/216-219 的量级是 1e-2 ~ 1，float16 完全够）。`seq_hist` 的 int16、`mask` 的 int8 可以保留（值域安全）。 |
| S0-3 | `rl_ppo_trainer.py:555` | `action_id == 46` → `action_id == 48`。**同时建议整条立直奖励先删掉**（见 S5-3）。 |
| S0-4 | `rl_ppo_trainer.py:1009` | 截断处（`dones[-1]==False`）额外前向一次真正的 `V(s_T)` 作为 bootstrap，而不是复用 `values[-1]`。 |

**验收**：随便跑 20 iter，采样时记录的 `log_prob` 与 update 首个 epoch 重算的 log_prob 之比 `ratios.mean()` 应 ≈ 1.0（加一行 debug 打印，第 0 个 epoch 应落在 0.99~1.01）。现在这个值必然远离 1。

---

### 阶段 1 —— 打通"会和牌 / 会过牌"通路（最高优先级，1~2 天）

这是把项目从"必死"拉回"能训"的关键一步，**不需要重建数据集就能先做**。

#### S1-1 建立唯一动作真源 `actions.py`

```python
# actions.py —— 唯一真源，禁止在其它文件硬编码动作数字
from pymahjong import MahjongEnv as _E

DISCARD      = range(0, 34)     # 0..33  普通打牌
DISCARD_RED  = range(34, 37)    # 34..36 赤5m/5p/5s 打出
CHI          = range(37, 43)    # 37..42 左/中/右 × (不用赤/用赤)
PON          = range(43, 45)    # 43,44  普通/用赤
ANKAN, MINKAN, KAKAN = 45, 46, 47
RIICHI, RON, TSUMO, KYUSHU = 48, 49, 50, 51
PASS_RIICHI, PASS_RESPONSE = 52, 53
N_ACTIONS = 54

# 启动即断言，防止 pymahjong 版本漂移
assert (_E.CHILEFT, _E.PON, _E.ANKAN, _E.MINKAN, _E.KAKAN,
        _E.RIICHI, _E.RON, _E.TSUMO, _E.PUSH,
        _E.PASS_RIICHI, _E.PASS_RESPONSE) == (37, 43, 45, 46, 47, 48, 49, 50, 51, 52, 53)
```

`data_builder` / `supervised_trainer` / `rl_ppo_trainer` 全部改成 import 它。同时补一份 `tests/test_action_semantics.py`：对 0~53 每个 id 断言其 `ACTION_TYPES[i]` 与语义表一致。

#### S1-2 修掉 `supervised_trainer.py:354` 的定时炸弹

`act_map = [45,34,37,39,41,42]` 是 **47 维空间**的映射，而头已经是 54 维。改成 54 维语义（`PASS_RESPONSE=53, CHI=37, PON=43, KAN=46, RIICHI=48, HORA=49/50`），并在阶段 2 重建标签后细化到具体 variant。**在改完之前，绝对不要重训 SL。**

#### S1-3 修正 `adapt_policy_state_dict`（`rl_ppo_trainer.py:325-393`）

对照 pymahjong v1.0 的 47 动作表（PDF Table 5），现有映射有 3 处错：

| 54 维目标 | 现在取 | 应取 |
|---|---|---|
| 50 Tsumo | `w[42]`(Ron) | `w[43]`(Tsumo) |
| 51 九种九牌 | `w[45]`(Pass) | `w[44]`(Kyushukyuhai) |
| 52 不立直 | `w[45]`(Pass) | `w[46]`(不立直) |

（37~42 全取 `w[34]`、45~47 全取 `w[39]` 是可接受的近似，但 v1.0 里 35/36=Chi 中/大、38=AnKan、40=KaKan 也是可用的，建议一并精确映射。）

#### S1-4 **复活死动作头**（本方案的核心动作）

即使映射全对，41~46 这些权重本身是死的。因为源数据里根本没有这些样本，PPO 又永远碰不到和牌，所以必须人工破局。三选一，**推荐 A+B 组合**：

- **A. 头行重置 + 先验热启动**（不改架构，只改权重初值）：
  对 `policy_out` 的第 48/49/50/51/52/53 行重新初始化（与 0–33 行同尺度的正态），并把 `bias[RON]`、`bias[TSUMO]` 设为 **+2.0**，`bias[PASS_RESPONSE]` 置 0。
  理由：日麻中"能和就和"在绝大多数场合接近最优，用它做先验是安全的，PPO 之后可以自己学会见逃。
- **B. KL 惩罚屏蔽**：`update_from_buffer` 里算 KL 时，把 48~53 这几维从 `sl_probs` 的支撑集里剔除（或对这几维单独把 `kl_beta` 置 0）。否则 KL 会把刚复活的头又拉回死先验。
- **C. 兜底诊断开关**：加一个 `FORCE_AGARI=True` 的调试模式 —— 只要 mask 里有 RON/TSUMO 就强制执行，用于确认"打通之后奖励管线是否正常"。**只用于诊断，不能用于正式训练。**

#### S1-5 抑制"逢鸣必鸣"

在阶段 2 补上 PASS 负样本之前，先用一个临时的、可配置的鸣牌门槛（例如对 37~47 的 logit 减去常数 `call_penalty`，做一次 sweep 找到把鸣牌命中率压到 25~35% 的值）。**明确标注为临时脚手架**，阶段 4 SL 重训后必须删掉。

**阶段 1 验收标准**：
- 重跑 §1.3 探针：`ron_taken / ron_avail ≥ 0.95`，`tsumo_taken / tsumo_avail ≥ 0.95`。
- 1500 个 p0 决策步内 `riichi_avail > 0`（说明门前率恢复）。
- 跑 200 iter PPO 后，训练日志 `WinRate` **> 5%**（现在是 0.003）。
- 2500 半荘评估：和了率 > 15%，`avg_net` 与 `avg_rank` 符号一致（现在 rank 1.936 / net -355 的矛盾会消失）。

---

### 阶段 2 —— 数据与标签管线重建（3~5 天 + 重建算力）

#### S2-1 真实 value 目标（`data_builder.py:274`）

MJAI 事件里就有：`{"type":"hora","deltas":[-8000,0,0,9000]}`、`{"type":"ryukyoku","deltas":[-1000,-1000,3000,-1000]}`。

```python
elif type_str in ["hora", "ryukyoku"]:
    score_changes = event.get("deltas", [0, 0, 0, 0])
    flush_kyoku_buffer(score_changes)
```

同时建议同步保存**两个** value 目标：`t_score_hand`（本局点数变动 / 10000）与 `t_score_final`（半荘终局顺位点）。前者稠密好学，后者才是真目标；SL 先学前者，RL 用后者。注意 MSE 对麻将点数的重尾分布不稳，改用 Huber（`smooth_l1`）。

#### S2-2 真实 tenpai / danger 标签（`data_builder.py:152-153`）

`current_hands_snapshot` 已经存了三家真实手牌，`calculate_waits_matrix` 已经在算 waits —— **tenpai 标签几乎是免费的**：同一次 shanten 计算里 `shanten == 0` 就是听牌，直接填 `t_tenpai[rel_p-1]`。

`t_danger` 建议**重新定义**：不做 binary，做成"若此刻打出牌 t，被 rel_p 家荣和的概率"，即 `waits[rel_p][t] × P(该家听牌)`，用同一份 oracle 手牌算即可，得到 [0,1] 连续值，配 BCE 依然可用。**在标签实装前把 `lambda_danger` 置 0**（现在是 1.0，正在优化一个全零目标）。

另外，`calculate_waits_matrix` 对**副露手是算错的**：`mahjong` 库的 `calculate_shanten` 期待包含副露在内的 13/14 张等价手牌，这里只传了门前部分并靠 `sum % 3 == 1` 放行 → 副露局面的向听数与待牌全错。修法：把副露还原成面子后一起传入，或对副露手单独处理。

#### S2-3 补齐缺失的样本类型（`data_builder.py:234` 附近）

| 缺什么 | 怎么补 |
|---|---|
| `ankan` / `kakan` 事件 | 加进 elif 分支，并 `player.meld_types.append(...)`（现在 `meld_types` 从未被 append） |
| **PASS 负样本** | 每次别家 dahai 后，对其余三家检查"是否可以吃/碰/杠/荣"，若可以但日志里没有该鸣牌事件，就写入一条 `t_act = PASS_RESPONSE` 样本。**这是修掉"逢鸣必鸣"的根本手段。** |
| **RIICHI 样本** | `reach` 事件 → 在其前一个 dahai 之后写入一条 `t_act = RIICHI`；同时在"可立直但没立直"的局面写 `PASS_RIICHI` 负样本 |
| **HORA 样本** | `hora` 事件 → 写入 `t_act = RON`（有 target）或 `TSUMO`（target==actor）；"可荣和但见逃"的局面写 `PASS_RESPONSE` 负样本 |
| Chi 三变体 | 由 `consumed` 推出是左/中/右吃、是否用赤 → 精确映射到 37~42，而不是统统 `ACTION_CHI` |

**这一条做完，阶段 1 的 S1-4/S1-5 脚手架才可以拆掉。**

#### S2-4 补齐 GameState 的死字段

`data_builder` 目前只填 `round_wind / honba / kyotaku / dora`。以下全部可从日志直接取：

- `self_wind = (seat - start_kyoku.oya) % 4`（现在恒为东）
- `players[i].score = start_kyoku.scores[i]`，并按 deltas 逐局更新（现在恒 25000）
- `is_all_last`（东风战：`bakaze==E and kyoku==4`；半荘：`bakaze==S and kyoku==4`）（现在恒 False）
- `tiles_left = 70 - 已发生的 tsumo 事件数` —— **现在恒为 70，等于模型两侧都看不到巡目。押し引き判断没有巡目信息基本等于瞎打。**

#### S2-5 存储方案（决定你能不能用满 184425 个文件）

实测 `state_2d` 中非 0/1 的元素占比 **0.0001**，且 221~255 共 35 个通道**完全没有被写过**。

推荐（**不改网络输入形状**，只改磁盘表示，在 `Dataset.__getitem__` 里还原成 256×4×9 float32）：

| 数据集 | 内容 | dtype | 每样本 |
|---|---|---|---|
| `state_bin` | 通道 0–210 | `uint8`（值域 0–4） | 7 174 B |
| `state_ctx` | 通道 211–220 的 10 个标量 | `float32` | 40 B |
| （通道 221–255） | 恒 0，**不存**，加载时补零 | — | 0 B |

配合 `chunks=(256, ...)` + `compression="lzf"`，预计 **15~25×** 压缩：2.4 M 样本从 91 GB 降到 4~7 GB。这样用满 184425 个文件（约 9000 万样本）也只需 ~200 GB，磁盘（剩余 657 GB）放得下。

顺带：现在 `chunks=(128,256,4,9)` ≈ 4.7 MB/chunk 且无压缩，`shuffle=True` 下每读一个样本要拉整块 4.7 MB —— 这同时是 SL 训练的 IO 瓶颈。

#### S2-6 数据集卫生

- **game-level 划分**：现在是 `selected_files[:split_idx]`（`data_builder.py:317`）按文件顺序切，抽样也是 `mjson_files[:max_files]`。改为先 `shuffle(manifest, seed=固定)`，再按**对局**切分 train / val / **test**（test 永不参与训练与调参）。
- 每条样本存 `game_id / kyoku_id / step_id / actor`。
- HDF5 attrs 写入 `schema_version / feature_version / builder_git_hash / source_manifest_sha256 / build_time`。
- build 结束打印统计报告：样本量、各动作分布、各标签正例率、局面阶段（序盘/中盘/终盘）分布。
- parser invariant 检查：每家手牌数、每种牌 ≤4、`sum(deltas)` 守恒、鸣牌合法性；不通过就丢弃该局并计数。
- `data_fetcher.py`：加 SHA256 校验与 retry/backoff；源 URL 移入 config。

---

### 阶段 3 —— 双管线特征一致性（与阶段 2 并行，2~3 天）

**先写测试，再改代码。**

#### S3-1 黄金一致性测试（最高工程 ROI）

`tests/test_encoder_parity.py`：构造同一个局面，分别喂给
`MahjongFeatureExtractor256.extract(game_state)` 与 `decode_obs_93_to_256(env.get_obs(p), scores, p)`，
**逐通道 `np.allclose`，逐 token 比较 `seq_hist`，逐维比较 `cond_vec`**。跑 1000 个随机局面。

这一个测试会一次性暴露下面所有条目，而且以后每次改特征都有保护。

#### S3-2 已知需要对齐的点

| # | 位置 | 修法 |
|---|---|---|
| A | `rl_ppo_trainer.py:493` | `state[216+i]` → `state[216 + rel_seat]`，`rel_seat = (i - p_id) % 4` |
| B | `rl_ppo_trainer.py:487` 之后 | 补 213(本场) / 214(供托) / 215(残り牌) / 220(オーラス)。残り牌需要 wrapper 自己数巡目 |
| C | `rl_ppo_trainer.py:420`（N-5） | 副露通道按 chi / pon / ankan / minkan 分槽。obs_93 的 `6+i*6` 那 4 个平面区分不了类型，需要 wrapper 自己维护鸣牌历史（`step()` 里已经能看到 action_id，直接记录即可） |
| C' | `rl_ppo_trainer.py:433`（N-6） | `st_base+9..12` 改为真正的"立直后舍牌" |
| D | `rl_ppo_trainer.py:437`（**N-2，纯 bug**） | `vis` 不能用"≥1 指示位"求和。用 obs 的枚数平面（0–3 / 6–9 / 30–33 …）按 `sum(plane_k)` 还原真实可见枚数，或直接在 wrapper 里维护 `visible_counts[34]` |
| E | `rl_ppo_trainer.py:638` | 补 `cond_vec[12..15]` |
| F | seq_hist（N-3 / N-4） | 定一份**唯一 token 规范**并写进 `docs/`：`token = tile_id*8 + rel_actor*2 + cut_type`，`272 = PAD`。四处改到一致：①`data_builder.py:210` 的 `cut_type` 必须是**每个历史 token 各自的**手切/摸切；②`data_builder.py:244` 鸣牌分支不能用另一套 `raw_seq*8`；③`feature_extractor.py:348` 与 `rl_ppo_trainer.py:648` 的截断方向统一为**最近 72 手**；④`feature_extractor` 的 seq_hist 必须携带 `rel_actor`（现在恒 0） |

#### S3-3 数据增强修正（`supervised_trainer.py:165-195`）

- `new_state[perm[i]+4] = temp_red[i]` → 用 `perm` 的**逆置换**（两个三轮换会错，约 1/3 的增强样本赤宝牌通道错位）。
- 花色置换必须**同步变换 `t_waits`（102=3×34）与 `t_danger`**，现在只同步了 `target_discard`。
- 花色置换必须**同步变换 `seq_hist` 的 tile_id 部分** —— 现在 `state_2d` 变了而序列 token 没变，同一个样本内部两个模态语义不一致。
- **删掉数字反转（flip）**：宝牌指示牌的 `n → n+1` 关系反转后变成 `n → n-1`，它不是麻将的合法对称，模型学到的是矛盾的宝牌推断。
- 增加 `tests/test_augmentation_invariance.py`：置换后重新解码，断言手牌 / 副露 / 舍牌 / 宝牌 / waits 全部自洽。

#### S3-4 其它工程测试

- **规则回归**：固定 100~1000 个牌谱做黄金用例，覆盖七対子 / 国士 / 四杠 / 岭上 / 抢杠 / 一炮多响 / 流局 / 立直 / 赤宝牌。
- **动作 mask 一致性**：对 0~53 每个 id，断言 `_get_mask()` 与 `env.get_valid_actions()` 完全一致。
- **模型数值**：shape / dtype / NaN / Inf / TorchScript parity / DirectML vs CPU parity；`DirectMLSafeAdamW` 与标准 AdamW 做 parity test。
- `torch.jit.trace(..., check_trace=False)`（`rl_ppo_trainer.py:693`）在开发期改成 `True`。

---

### 阶段 4 —— SL 重训（架构不变，1~2 天 + 训练时间）

前置：阶段 1/2/3 全部完成，且黄金一致性测试通过。

| # | 改动 |
|---|---|
| S4-1 | policy 头保持 `Linear(1024, 54)` 不变；`act_map` 改成 54 维真语义（S1-2），并用 S2-3 产出的精确 variant 标签 |
| S4-2 | **加入合法动作 mask**。现在 `p_out_masked = p_out + (1-valid_mask.unsqueeze(1))*-1e9`（`supervised_trainer.py:384`）里的 `valid_mask` 是**样本级**开关（0 或 1），不是动作级合法性掩码 —— 等于让模型在全部 54 类上做无约束分类。数据集需要一并存下每个样本的 54 维 legal mask |
| S4-3 | `lambda_danger` 在 danger 标签实装前置 0；五个 loss 权重做一次 grid 或 dynamic weighting |
| S4-4 | 验证指标分头记录：policy top-1 / top-3 / legal-action accuracy、value MAE、tenpai AUC、waits AP、danger AUC。现在只看 total loss |
| S4-5 | `t_waits` 正例率 0.75%，加 `pos_weight` 或 focal loss |
| S4-6 | value loss 用 Huber 替代 MSE |
| S4-7 | **保存一份 immutable 的 `sl_baseline_frozen.pth`**，作为所有后续实验的永久对照锚点 |

**验收**：
- discard top-1 accuracy > 60%（Mortal 级参考 ~68%），top-3 > 85%
- 鸣牌决策（含 PASS）accuracy > 80%
- RIICHI / RON / TSUMO 三个 logit 的 `‖w‖` 与 0–33 同量级（**不再是 0.308**）
- 用 §1.3 探针跑纯 SL 模型：鸣牌命中率落在 25~40%，能和必和率 > 95%

---

### 阶段 5 —— RL 环境 / 奖励 / 评估重建（3~5 天）

#### S5-1 真正的半荘（`rl_ppo_trainer.py:512-520`）

`pymahjong` 的 `reset()` **支持** `oya / game_wind / scores / kyoutaku / honba / seed`（`env_pymahjong.py:130-155`）。当前 `_reset_hand_internal` 什么都不传，导致：

- 点数每局回到 25000（外层 `self.scores` 只是个影子账本，与 env 内部脱节）
- 本场 / 供托不继承 → **立直棒每局凭空消失**（这正是 eval log 里 `avg_rank 1.936` 却 `avg_net -355` 的原因：四家都不会和牌，全部流局，立直棒持续泄漏，总分不守恒）
- `game_wind` 恒为 east → 不存在南场与オーラス，顺位打法原理上学不到

改成维护 `oya / honba / kyotaku / scores` 并逐局传入。这是**改动最小、收益最大**的一处修复。

#### S5-2 非法动作 → assert（`rl_ppo_trainer.py:530-536`）

改为 `raise`（debug 模式）或至少不要用假 `done=True` 截断轨迹。

#### S5-3 奖励重构

- `gamma`：`0.99` → **1.0 或 0.999**。p0 一个半荘约 150+ 决策步，`0.99^150 ≈ 0.22`，顺位奖励 `[1.2,0.3,-0.1,-1.8]` 传到序盘几乎为零。
- **删掉无条件的 RIICHI +0.1**（`rl_ppo_trainer.py:557`）。相对于"満貫自摸 ≈ +0.8"这个量级过大，会诱导滥立直。
- ukeire shaping 在 `async_environment_worker` 中**没有做 phase 门控**（shanten shaping 有），Phase 3 号称"纯 RL"但它一直开着 —— 补上门控。
- **reward decomposition logging**：把 `score / rank / shanten / ukeire / riichi / illegal` 每一项单独累加并写 CSV。现在只有一个标量，出了问题无法归因。
- 做一次 **Reward Ablation Matrix**：A=终局点数 / B=+顺位 / C=+shanten / D=+ukeire / E=+riichi / F=full。
- `ukeire` 的 34 次 `calculate_shanten` 是采样吞吐的主要开销，做缓存或降频。

#### S5-4 Phase 判据

- 平台期判据换掉 CV（`rl_ppo_trainer.py:1431`）：改用**最近 N 次评估顺位的回归斜率 + 残差标准差**（斜率项已经有了，把 CV 那一半换掉即可）。
- `2.40 / 2.45 / -1200 / 0.35 / 0.05` 全部移入 `config/training.yaml`。
- **区分"训练控制指标"与"科学评估指标"**：phase 转移可以用 rolling baseline，但**报告成绩必须对 immutable `sl_baseline_frozen.pth`**。现在 phase 1→2 把 baseline 换成了自己的毕业模型（`rl_ppo_trainer.py:1465`），benchmark 会持续漂移，`avg_rank` 在不同 phase 之间不可比。

#### S5-5 评估

- 固定 opponent pool：`{immutable SL, 历史最佳 RL, heuristic 牌效 baseline, 当前 rolling baseline}`。
- 报告 **bootstrap 置信区间** + 顺位分布（1/2/3/4 位比例），不要只报均值。
- 半荘数按 effect size 动态决定；日常 300~500 半荘即可，里程碑再跑 2500（现在每 500 iter 跑 2500 半荘，是小时量级的开销）。
- 加行为风格指标：和了率 / 放铳率 / **副露率** / 立直率 / 平均和了打点 / 平均和了巡目。**副露率是这次事故的直接哨兵指标，必须常驻监控。**
- 至少 3 个独立 seed；保存 per-game 结果以便事后重算 CI。

#### S5-6 断点续训完整性

- checkpoint 存成字典：`{model, optimizer, scheduler, phase, iteration, kl_beta, rng_state, config, dataset_manifest}`。当前只存 `model.state_dict()`，从 `TRUE_BEST`（文件名无 `_iter`）恢复时 `it` 归 0，紧接着 `logger.truncate_after(0)` 会**把整个 CSV 截断**。
- 固定 Python / NumPy / PyTorch / DataLoader worker 的全部随机种子。
- `plot_metrics.py:149,181` 的 `set_yticklabels(get_yticks())` 未配 `set_yticks`，matplotlib 会告警且刻度可能错位。

---

### 阶段 6 —— 后续研究（P0~P2 全部清完之后再做）

两份参考书在这一段的判断一致，直接采纳：**信念模型（Opponent Belief Model）是下一阶段最有价值的方向**，而不是把 Transformer 从 4 层加到 8 层。

数据管线里已经有 oracle 手牌 → 真实 waits 标签，teacher signal 是现成的：
`公开信息 → Belief Encoder → P(对手待牌 / 手牌形 / 立直圏) → Policy`。

其余：SIL / Hero Buffer 按轨迹质量加权与去重、league / population 训练、Elo 评级、offline RL、search-assisted policy。

---

## 4. 架构冻结清单（本方案**不**动的东西）

| 项目 | 状态 |
|---|---|
| 256×4×9 状态张量 schema | 冻结（只改**内容正确性**与**磁盘表示**） |
| 18 × FiLM-ResBlock2D，256 通道 | 冻结 |
| 4 层 DirectMLSafeTransformerLayer，embed 256 | 冻结 |
| Cross-Attention 融合，1024 latent | 冻结 |
| `policy_out = Linear(1024, 54)` + value + 3 aux 头 | 冻结（只改**标签语义**与**权重初值**） |
| dropout 0.30 / BN / res_scale 0.1 | 冻结 |
| PPO + KL-to-SL + Hero Buffer/SIL + 三阶段课程 | 冻结（只改**判据与奖励构成**） |

> 唯一一处"擦边"项：`DiscardSequenceEncoder` 缺 `key_padding_mask`，72 个位置里大部分是 272 padding，加了位置编码后仍参与 attention。这**不增减任何参数**，但会改变 forward 语义。建议**留到阶段 4 SL 重训时作为单独的 A/B 实验**，而不是混在这轮修复里。

---

## 5. 执行顺序与验收总表

| 阶段 | 内容 | 工作量 | 关键验收 |
|---|---|---|---|
| **0** | 4 处止血（eval 模式 / int8 / action 46→48 / GAE） | 1 h | 首个 epoch `ratios.mean() ∈ [0.99, 1.01]` |
| **1** | actions.py + 修 act_map/adapt + 复活死动作头 + KL 屏蔽 | 1–2 d | 探针：能和必和 >95%；200 iter 后 WinRate >5% |
| **2** | deltas→value、tenpai/danger 真标签、ankan/kakan、PASS/RIICHI/HORA 样本、GameState 死字段、uint8+lzf 存储、game-level split + test set + manifest | 3–5 d + 重建 | 数据体检脚本：各标签正例率合理、动作分布含 RIICHI/HORA/PASS、磁盘 <10 GB / 2.4 M 样本 |
| **3** | 黄金一致性测试 + 8 处特征对齐 + 增强修正 + 规则/mask/模型测试 | 2–3 d（与 2 并行） | `test_encoder_parity` 1000 局面全绿 |
| **4** | SL 重训（架构不变）+ 分头指标 + immutable baseline | 1–2 d + 训练 | discard top-1 >60%；48/49/50 的 ‖w‖ 与 0–33 同量级 |
| **5** | 真半荘环境、奖励分解与 ablation、平台期判据、benchmark pool、CI、多 seed、完整 checkpoint | 3–5 d | avg_rank 与 avg_net 符号一致；对 immutable SL 的成绩跨 phase 可比 |
| **6** | Belief model / league / 研究项 | — | — |

**依赖关系**：`0 → 1 →（2 ∥ 3）→ 4 → 5 → 6`。

阶段 1 之后可以**先重启一轮 PPO 观察和了率是否起来**，作为整条链是否修对的快速信号；但正式的实验基线必须等阶段 4 之后重新建立 —— **在此之前的所有 `avg_rank` 数字都不可信，包括 phase2 的 1.936。**

---

## 6. 一句话总结

> 你现在缺的不是更强的网络，而是**"模型从来没被教过怎么和牌，也从来没被教过怎么不鸣牌"**。
> 把这两件事教会（阶段 1 + 2），其余所有问题才谈得上是"调优"。
